
  # Museo de Historia Home

  This is a code bundle for Museo de Historia Home. The original project is available at https://www.figma.com/design/6zoOvf3SqILEShpInb8Mrc/Museo-de-Historia-Home.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  npm run dev
  git add -f dist/
git commit -m "Add dist folder for GitHub Pages"
git push origin main
